// Business action types
export enum BusinessActionType {
  UPDATE = 'UPDATE',
  SET = 'SET',
}

// Appointment action types
export enum CustomerAppointmentActionTypes {
  ADD_APPOINTMENT = 'ADD_APPOINTMENT',
  UPDATE_APPOINTMENT = 'UPDATE_APPOINTMENT',
  REMOVE_APPOINTMENT = 'REMOVE_APPOINTMENT',
}

export enum TechnicianAppointmentActionTypes {
  UPDATE_APPOINTMENT = 'UPDATE_APPOINTMENT',
  REMOVE_APPOINTMENT = 'REMOVE_APPOINTMENT',
}

export enum AdminAppointmentActionTypes {
  UPDATE_APPOINTMENT = 'UPDATE_APPOINTMENT',
  REMOVE_APPOINTMENT = 'REMOVE_APPOINTMENT',
}

// Service action types
export enum ServiceActionTypes {
  ADD_SERVICE = 'ADD_SERVICE',
  UPDATE_SERVICE = 'UPDATE_SERVICE',
  REMOVE_SERVICE = 'REMOVE_SERVICE',
}

// Store types
// [TODO]
